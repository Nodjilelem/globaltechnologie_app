import 'package:flutter/material.dart';

class AntennesParaboliquesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Antennes Paraboliques")),
      body: Center(child: Text("Contenu de la formation en antennes paraboliques")),
    );
  }
}
