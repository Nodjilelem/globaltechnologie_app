import 'package:flutter/material.dart';

class PanneauxSolairesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Panneaux Solaires")),
      body: Center(child: Text("Contenu de la formation en panneaux solaires")),
    );
  }
}
