import 'package:flutter/material.dart';

class DepannageTelephoneScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Dépannage Téléphone")),
      body: Center(child: Text("Contenu de la formation en dépannage de téléphone")),
    );
  }
}
