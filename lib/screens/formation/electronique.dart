import 'package:flutter/material.dart';

class ElectroniqueScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Électronique")),
      body: Center(child: Text("Contenu de la formation en électronique")),
    );
  }
}
