import 'package:flutter/material.dart';

class InformatiqueScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Informatique")),
      body: Center(child: Text("Contenu de la formation en informatique")),
    );
  }
}
