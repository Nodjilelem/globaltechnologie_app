import 'package:flutter/material.dart';

class ElectriciteIndustrielleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Électricité Industrielle")),
      body: Center(child: Text("Contenu de la formation en électricité industrielle")),
    );
  }
}
